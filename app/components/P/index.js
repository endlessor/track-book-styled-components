import styled from 'styled-components';

const P = styled.p`
  object-fit: contain;
  font-size: 18px;
  font-weight: normal;
  font-style: normal;
  font-stretch: normal;
  line-height: normal;
  letter-spacing: normal;
  color: #8d9096;
`;

export default P;
