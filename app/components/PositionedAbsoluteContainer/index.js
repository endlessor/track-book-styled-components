import styled from 'styled-components';

const PositionedAbsoluteContainer = styled.div`
  position: absolute;
  background-color: ${props => props.backgroundColor};
`;

export default PositionedAbsoluteContainer;
