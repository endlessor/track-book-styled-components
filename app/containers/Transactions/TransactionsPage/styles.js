import styled from 'styled-components';
import { PaddedContainer, FlexStartContainer } from '../../../components';

export const TransactionsContainer = styled(PaddedContainer)`
  height: 100%;
  overflow: hidden;
`;

export const TabsContainer = styled(FlexStartContainer)`
  padding: 0 38px;
  background-color: #f4f5f8;
`;
